- CentOS
```
yum install wget -y && wget -O install.sh https://d.hws.com/free/hwslinuxmaster/script/install.sh && bash install.sh
```

- Ubuntu
```
apt-get update && apt-get install wget sudo -y && wget -O install.sh https://d.hws.com/free/hwslinuxmaster/script/install.sh && sudo bash install.sh
```
